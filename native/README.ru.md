# native/

[English](README.md) | [中文](README.zh.md) | Русский

Нативные исходники и публичные пакеты, сопровождаемые вместе с DeepSeek Harness. [Workspace `landlock-run/`](landlock-run/README.ru.md) владеет лончером самограничения Landlock с последующим запуском, потребляемым harness, — включая его архитектуру, семейство из трёх npm-пакетов, поддержку платформ, процесс разработки и [процедуру выпуска](landlock-run/docs/release.md).

## Границы workspace и выпуска

`landlock-run/` и его пакеты принадлежат корневому pnpm-workspace и lockfile репозитория. Потребители harness во время разработки и CI используют текущую entry-запись из workspace, поэтому изменение контракта лончера и обновление его потребителя могут попасть и быть проверенными вместе.

Workflow `Landlock Run` главного репозитория собирает и тестирует каждую поддерживаемую архитектуру. `Landlock Run Release` собирает эти нативные артефакты, пакует и проверяет три npm-tarball'а, затем опционально публикует их под одной версией лончера. Entry-пакет удерживает платформенные пакеты как npm optional dependencies, так что npm по-прежнему устанавливает только пакет под операционную систему и CPU пользователя.
