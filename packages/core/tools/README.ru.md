# dsh-tools

[English](README.md) | [中文](README.zh.md) | Русский

Реестр инструментов и пайплайн их исполнения. Инструментальные плагины регистрируют свои схемы и исполнителей; цикл агента проводит каждый вызов через `tools/pre-execute` (расширяемую точку решения allow/deny) → монотонных зарегистрированных стражей → `tools/execute` (around-обёртку диспетчеризации для плагинов таймаутов, повторов и метрик) → `tools/post-execute` (осмотреть/заменить результат, присоединить контекст) → принадлежащую определению границу `finalizeContent` → уведомление `tools/result`, доступное только для наблюдения. Реестр также владеет тем, КАК его инструменты предъявляются модели: его конфигурация `mode` выбирает нативный вызов функций, [Code Mode](#code-mode) или оба варианта сразу, а отдельный агент перекрывает это умолчание для себя методом `presentAs`.

## Сервис: `ToolRuntime` (ключ контекста: `tools`)

### Конфигурация

```yaml
tools:
  mode: native   # native (default) | code | both
```

`native` публикует видимые инструменты как определения функций. `code` добавляет зарезервированный транспорт `run_code`, генерируемую секцию `tools:sdk` и правило `tools:code-only`, согласно которому напрямую можно вызывать только `run_code`, — исполнитель затем обеспечивает его, сворачивая прямой вызов модели, называющий любой другой инструмент, в `UNKNOWN_TOOL` ещё до запуска политики; `both` добавляет обе формы и никакого такого правила не провозглашает, потому что его нативные вызовы действительно исполняются. Это умолчание для агентов, не объявивших собственного выбора, — пресет агента избирает своё через [`dsh-agent-tool-presentation`](../agent-tool-presentation/README.md). Зарезервированный транспорт нельзя зарегистрировать, перекрыть, ограничить или удалить, и его имя остаётся занятым при любом настроенном режиме, ведь любой агент может избрать кодовый режим. Ненативные режимы требуют `ctx.codeRuntime`, чей `language` имеет зарегистрированный рендерер SDK, — TypeScript поставляется через [`dsh-code-runtime-worker-thread`](../../code-runtime/code-runtime-worker-thread/README.md), рендерер Python встроен и управляет любым рантаймом, сообщающим `language: 'python'` (штатный бэкенд `dsh-code-runtime-python` поставляется отдельно). Язык рантайма без рендерера громко валит сборку промпта, а запись `systemPrompt.toolOrder` для инструмента, которого режим не добавляет, отвергает сборку промпта. Слушатель `system-prompt/assemble` может заменить вклады реестра; возвращённая им сборка авторитетна, поэтому именно такой слушатель отвечает за сохранение пригодного протокола Code Mode.

### Публичный API

- `ctx.tools.register(definition: ToolDefinition): () => void` регистрирует доверенное типизированное внутрипроцессное определение с обязательным каноническим объявлением `output`. Слой — это scope вызывающего контекста: обычный контекст плагина регистрирует глобально, а `agent.ctx` агента — только для этого агента, перекрывая там одноимённый глобальный инструмент. Дубликаты имён в одном слое бросают исключение; ненативные режимы вдобавок отвергают зарезервированное имя транспорта `run_code`. Отсутствующее или неподдерживаемое объявление вывода, а также неположительный или нефинитный `timeoutMs` отклоняются при регистрации. Необязательный синхронный колбэк `finalizeContent` снимается в снапшот при старте вызова и может заменить только финальное содержимое, адресованное модели, после нормализации каждого исхода конвейера — включая ошибку, обнаруженную при материализации другого поля результата. Освобождается вместе с fiber вызывающего.
- `ctx.tools.presentAs(mode: ToolPresentationMode): () => void` выбирает этому агенту предъявление модели, перекрывая конфигурацию `mode` только для него; из обычного контекста он бросает исключение (общепроцессное предъявление — поле конфигурации), равно как и второе объявление в том же scope. Кодовый режим вдобавок регистрирует этому агенту его собственную секцию `tools:sdk`. Каталог не меняется — `schemas(agent)` по-прежнему сообщает о возможностях агента; схлопываются только инструменты в сборке. Освобождается вместе с fiber вызывающего.
- `ctx.tools.restrict(filter)` накладывает на глобальные инструменты маску allow/deny в объёме агента и из обычного контекста бросает исключение. Фильтр снимается в снапшот при регистрации; несколько масок пересекаются, а инструменты, локальные для scope, подмешиваются после. Маски deny допускают появившиеся позже глобальные инструменты, которых они не называют, маски allow исключают любые позднейшие имена. Неизвестные, локальные или зарезервированные имена и пустые фильтры отвергаются. Это живая композиция видимости, а не граница полномочий; см. [non-goal о безопасности и полномочиях](../../../.agents/notes/implemented/architecture/2026-07-08-agent-scope-contexts.md#security-and-authority-are-non-goals).
- `ctx.tools.get(name: string, scope?: ScopeKey): ToolDefinition | undefined` — разрешение таким, каким его видит один scope (перекрытия применены; вытесненный ограничением глобальный инструмент читается как отсутствующий); презентеры передают вызывающего агента, чтобы карточка отвечала тому, что исполнилось.
- `ctx.tools.schemas(scope?: ScopeKey): ToolSchema[]` — схемы всего, что видит scope (без функций `execute`). Схемы штатных инструментов каталогизированы в [docs/tool-catalog.md](../../../docs/tool-catalog.md), который генерируется загрузкой каждого инструментального плагина и съёмкой этого метода (см. [Agent Note о каталоге схем инструментов](../../../.agents/notes/implemented/process/2026-07-02-tool-schema-catalog.md)).
- `ctx.tools.guard(guard: ToolGuard): () => void` регистрирует монотонный синхронный страж исполнения после `tools/pre-execute`: возвращённая причина запрещает вызов, `undefined` оставляет его без изменений. Страж из обычного контекста действует глобально, страж из `agent.ctx` — только на этого агента. Позднейшие слушатели каскадов не могут превратить отказ стража обратно в разрешение. Освобождается вместе с fiber вызывающего.
- `ctx.tools.execute(exec)` без потерь снимает аргументы в снапшот и замораживает их, назначает непрозрачный токен, прогоняет полный конвейер политика/диспетчеризация/результат и независимо снимает авторитетный исход перед финальным наблюдением. Невалидные аргументы идут тем же путём результата, не достигая ни политики, ни тела. Around-обёртки могут заменять только `signal`; непосредственно перед телом реестр заново отказывает исходный сигнал вызывающего.
- `ctx.tools.executionMode(exec)` возвращает `parallel`, только когда классификатор `isConcurrencySafe(exec.arguments)` видимого определения вернул ровно `true`; неизвестная, скрытая, необъявленная, невалидная или бросающая классификация означает эксклюзивность.

### Внедряемые сервисы

`SystemPrompt` — реестр сам подаёт свои схемы инструментов в сборку системного промпта через `ctx.systemPrompt.tools()`. Seam одобрения вместо этого потребляется оппортунистически (`ctx.get('approval')`, без статического внедрения): развёртывание без него сохраняет деградацию ask→deny, и в обоих случаях реестр остаётся работоспособным.

### Отмена

Отмена кооперативна и завершается в quiescence. Каждое типизированное обращение передаёт принадлежащий вызывающему `AbortSignal`; тела инструментов получают его как обязательный readonly `exec.signal`, и лишь обёртки `tools/execute` могут временно заменить обязательный сигнал. Реестр сохраняет отмену вызывающего сквозь замену и никогда не соревнуется с начатым внутрипроцессным промисом на скорость. Отмена до вызова тела даёт `ABORTED_BEFORE_DISPATCH`; отмена после вызова способна заменить только успешный исход на `ABORTED`. Отказ стража, падение обёртки, ошибка инструмента, сбой после политики или принадлежащий таймауту `TOOL_TIMEOUT` остаются более специфичными. Заранее отменённый вход материализует и замораживает аргументы, затем пропускает все фазы политики и диспетчеризации и публикует один результат. Каждый асинхронный инструмент обязан наблюдать или пересылать сигнал и завершаться, только когда собственная работа остановлена. Полный контракт и предел жёсткого завершения описывает [Agent Note об отмене инструментов](../../../.agents/notes/implemented/architecture/2026-07-19-cooperative-tool-cancellation.md).

### События живого конвейера

Живой конвейер реестра — три преобразуемых каскада, затем принадлежащий определению финализатор содержимого, затем наблюдающее событие `tools/result`; изменения реестра намеренно остаются неотфильтрованными уведомлениями о разделяемом состоянии. Точные сигнатуры, режимы диспетчеризации, фильтрацию по scope и контракты локализации сбоев описывает генерируемая область [tools.ru.md](../../../docs/subsystems/tools.ru.md#cordis-surface), а полный порядок визуализирует генерируемый [пайплайн исполнения инструментов](../../../docs/tool-execution-pipeline.ru.md). `tools/result` — живое событие; похоже названное `tool/result` — долговечное событие сессии, которое цикл агента дописывает следом.

### Ключевые типы

- `ToolDefinition` — `ToolSchema` плюс обязательное `output { schema, render, presentationMeta? }` плюс `execute(args, exec)`, необязательные колбэки финального содержимого и презентации, кооперативный `timeoutMs` и необязательная классификация отдельного вызова `isConcurrencySafe(args)`. Тело возвращает только каноническое JSON-значение, объявленное схемой вывода, и кооперативно останавливается через `exec.signal`. `finalizeContent(exec, result)` исполняется ровно один раз для каждого нормализованного результата, включая сбои, обходящие пост-политику, и может заменить только `content`; он обязан быть синхронным и тотальным.
- `ToolExecutionInput` — переданное вызывающим описание вызова: `{ callId, name, arguments, signal, agent?, parent? }`; `signal` обязателен и readonly, вызывающий вправе передать непрозрачный токен объемлющего исполнения как `parent`, а собственный токен нового исполнения вызывающий не выбирает никогда.
- `ToolExecutionToken` — свежий брендированный `Symbol`, назначаемый реестром. Он поддерживает только корреляцию по равенству и никогда не пересекает границу модели, журнала или воркера.
- `ToolExecution` — readonly-представление конвейера: неизменяемые `{ token, callId, name, arguments, signal, agent?, parent? }`; исходный сигнал вызывающего реестр отдельно хранит и заново отказывает. `ToolDispatchExecution` — доступное только для `tools/execute` представление, чей обязательный сигнал изменяем: обёртка вправе заменить и вернуть его, но не удалить. `parent` вложенного вызова — это `ToolExecutionToken`, а не объект исполнения.
- `ToolRunContext` — исполнение, передаваемое телу инструмента, расширяющее `ToolExecution` методом `deferContext(context)`. Он откладывает один контекст до момента, когда финальный результат инструмента достигнет цикла, — обычно это контекст вложенной диспетчеризации, переносимый композитным инструментом, либо свежая инструкция плагинового происхождения, отчеканенная листовым инструментом (wrap-up из `tool-goal`), — и делает это даже когда инструмент затем бросает исключение или побеждает отмена; внедрить немедленно он не может никогда.
- `ToolExecutionResult` — дискриминированный исход в масштабе исполнения. Успех — `{ isError:false, value:JsonValue, content, meta?, additionalContexts? }`; неудача — `{ isError:true, error:{ message, info? }, content, meta?, additionalContexts? }` и значения не имеет. Идентичность вызова остаётся на неизменяемом `ToolExecution`. Реестр снимает, валидирует и замораживает каноническое значение до рендеринга, а долговечные поля презентации материализует до финального наблюдения. `ToolFailure.info` несёт внутренние `{ name, code }` для `HarnessError`; `additionalContexts` сохраняет каждый отложенный или выявленный post-execute `UserMessage` для FIFO после результата в цикле.
- `PreToolDecision` — `{kind:'allow'}` | `{kind:'deny', reason}` | `{kind:'ask', reason?}`. Переписывание входа намеренно не предлагается; `ask` обслуживается [`ctx.approval`](../../interaction/user-approval/README.md), когда тот смонтирован, иначе деградирует до deny.
- `PostToolDecision` — accept может заменить `content` или `value`, но никогда оба сразу, и может присоединить `additionalContexts`; block превращает обратную связь в неудачу без значения. Замена содержимого сохраняет каноническое значение и метаданные. Замена значения ревалидируется и перерисовывает содержимое и метаданные. Accept сохраняет отложенные инструментом контексты перед контекстами решения; block отбрасывает отложенные инструментом контексты и выставляет только те, что явно передало блокирующее решение.
- `ToolGuard` — `(execution) => string | undefined`; возвращённая строка — финальная монотонная причина отказа, оцениваемая после переставляемого каскада pre-execute и до диспетчеризации.
- `ToolCallView` / `ToolResultView` — независимые от провайдера намерения рендера с тегом `card`, которые инструмент возвращает из `presentCall` / `presentResult`, чтобы самому решать, КАК UI отображает ЕГО вызовы (см. «Презентация UI, принадлежащая инструментам»).

### Точки расширения

- Инструментальные плагины вызывают `ctx.tools.register()` — схемы автоматически стекаются в сборку.
- `tools/pre-execute` — переставляемая точка решения allow/deny/ask; `ctx.tools.guard()` добавляет после неё монотонную политику владельца.
- `tools/execute` оборачивает нормализованную каноническую диспетчеризацию ради таймаута, повтора или метрик. Обёртки могут заменять только операционный сигнал; успех, сочинённый обёрткой, нормализуется через разрешённое объявление вывода инструмента. Каждый канонический результат принадлежит одному неизменяемому токену диспетчеризации, поэтому кешированный результат другого вызова или инструмента ревалидируется по активному объявлению.
- `tools/post-execute` может заменить презентационное содержимое, заменить каноническое значение, заблокировать с обратной связью или присоединить упорядоченные контексты. Затем необязательный `finalizeContent` определения владеет своим последним инвариантом «только содержимое» и на нормальных результатах, и на внешних сбоях конвейера; `tools/result` наблюдает неизменяемый финальный исход. Замена содержимого — не граница конфиденциальности: блокируйте или заменяйте значение, когда программные потребители не должны его получать.
- Точные сигнатуры и порядок живут в генерируемой области [tools.ru.md](../../../docs/subsystems/tools.ru.md#cordis-surface) и [пайплайна](../../../docs/tool-execution-pipeline.ru.md).
- MCP-серверы: один плагин на сервер, обнаружение инструментов, вызов `ctx.tools.register()` со схемами сервера.

### Типизированные схемы параметров инструментов

Штатные авторы плагинов могут использовать помощник `defineTool()` (экспортируется этим пакетом) для типизированных схем параметров инструментов:

```ts
import { readFile } from 'node:fs/promises'
import type { Context } from '@deepseek-ai/cordis'
import { defineTool } from '@deepseek-ai/dsh-tools'

declare const ctx: Context

ctx.tools.register(defineTool({
  name: 'read_file',
  description: 'Read a file from disk.',
  parameters: {
    path: { type: 'string', required: true, description: 'Absolute file path' },
    offset: { type: 'number' },
    limit: { type: 'number' },
  },
  output: {
    schema: { type: 'string' },
    render: (_args, value) => [{ type: 'text', text: value }],
  },
  async execute(args, exec) {
    // args is typed: { path: string; offset?: number; limit?: number }
    return readFile(args.path, { encoding: 'utf8', signal: exec.signal })
  },
}))
```

Единый DSL схем использует `ParameterSchemaSpec` для неявного открытого объекта параметров и `ValueSchemaSpec` для любого корня JSON-значения. Он поддерживает `string`, `number`, `integer`, `boolean`, `null`, `array`, `object`, доступный только автору `json` и `oneOf` с ровно одной ветвью; скалярные значения `enum`/`const` проверяются на соответствие типу. Каждая явная DSL-запись декларирует `additionalProperties: true | false`, тогда как неявный корень параметров и сырой JSON Schema сохраняют стандартное открытое умолчание. Записи схем принимают только собственные перечислимые строковые ключи, а массивы схем обязаны быть плотными обычными массивами. Компиляция, валидация, отсоединение от реестра и рендеринг схема→TypeScript используют явные рабочие стеки, поэтому обработка валидных глубоких схем в рантайме ограничена памятью, а не стеком вызовов; `InferValue` сохраняет точные типы через 16 уровней контейнеров, а затем откатывается к `JsonValue`, так что сам TypeScript остаётся стеково-безопасным.

Определение `defineTool` валидирует аргументы модели до исполнения и превращает отсутствующие обязательные значения, неверные примитивы, недопустимые элементы enum и вложенные нарушения в `ToolArgsError` (`INVALID_ARGS`) для обычного пути ошибочного результата. Оно также выводит тип возврата тела и чистые проекторы вывода из `output.schema`; реестр снимает и валидирует возвращённые беспотерьные JSON до презентации. Неявный корень параметров открыт; явный объект принимает лишние ключи только при `additionalProperties: true`, а закрытый объект без объявленных свойств принимает только `{}`. Сырые объекты JSON Schema остаются открытыми, пока явно не задано `additionalProperties: false`. Значения по умолчанию не подставляются; открытые объекты без `properties` и массивы без `items` получают только проверку типа контейнера. Инструменты, зарегистрированные сырыми, сами владеют валидацией входа, но по-прежнему объявляют и получают контролируемый реестром вывод.

Подробности см. в публичном API: `defineTool`, `validateArgs`, `ToolArgsError`, `ValueSchemaSpec`, `ParameterSchemaSpec`, `InferValue`, `InferArgs`, `valueSchemaSpecToJsonSchema` и `parameterSchemaSpecToJsonSchema`.

Необязательный `timeoutMs` должен быть положительным и конечным; это метаданные политики, а не видимая модели схема.

Необязательный `isConcurrencySafe(args)` получает типизированные, мягко валидируемые аргументы. Ровно `true` разрешает параллельные диспетчеризацию и исполнение тела; невалидный вход и все прочие исходы остаются эксклюзивными. Избравшие это тела не мутируют состояние, принадлежащее родителю, а гонки разделяемого состояния обязаны коммутировать или fail-closed. Полный контракт безопасности описывает [Agent Note о параллельных вызовах инструментов](../../../.agents/notes/implemented/feature/2026-07-10-parallel-tool-call-execution.md).

### Обязательное подмножество сырого JSON Schema

`JsonSchemaNode` — сырой аналог, разделяемый выводами инструментов, генерацией Code Mode, субагентами и воркфлоу. Он допускает любой JSON-корень, несвязанный узел JSON только с аннотациями и `oneOf` с ровно одной ветвью; аннотации обязаны оставаться беспотерьным JSON. `assertSupportedJsonSchema()` отвергает неподдерживаемые конструкции, а `validateJsonSchemaValue()` возвращает нарушения с указанием пути. Субагенты и воркфлоу сохраняют требование объектного корня, заданное вызывающим, через `assertObjectJsonSchema()` и `ObjectJsonSchema`, а не через ограничение общего словаря.

### Презентация UI, принадлежащая инструментам

Инструменты опционально владеют чистыми намерениями рендера `presentCall()` и `presentResult()`, так что UI не заводит особых случаев по именам инструментов:

- Представления вызова — `{ card: 'generic', title, kind?, rawInput?, content?, locations? }`, `{ card: 'terminal', title, description?, cwd? }` или `{ card: 'diff', title, diffs, locations? }`.
- Представления результата — `{ card: 'generic', title?, content? }`, `{ card: 'terminal', title?, output?, exitCode?, signal? }`, `{ card: 'diff', title?, diffs }`, `{ card: 'search', shape, title?, truncated, total, … }` (завершённый поисковый запрос — сгруппированные по файлам совпадения для `shape: 'matches'` (grep) либо плоский список путей для `shape: 'paths'` (glob); `truncated`/`total` не дают UI выдать усечённый результат за полный; представление не несёт текста результата, и у поиска нет аналога `card: 'search'` на момент вызова), `{ card: 'read', title?, path, offset, lines, totalLines, lang?, content? }` (завершённое чтение файла → просмотр кода с номерами строк и опциональной подсветкой синтаксиса; `offset` — первая строка окна в нумерации с единицы, сохраняется даже при пустых `lines`; `lines` — `{ number, text }[]`, хранящее настоящие номера строк файла, а `content` — текст без конверта, к которому откатывается UI без поддержки чтения) или `{ card: 'web', kind: 'search' | 'fetch', title?, … }` (завершённое веб-извлечение; ветви `kind` несут структурные источники поиска либо сводку fetch, а UI без возможности `web` откатывается к сырому содержимому результата).

Возврат `undefined` избирает универсальный откат. Презентеры зависят только от своих аргументов и долговечного результата, потому что UI вызывает их и при живой трансляции, и при воспроизведении журнала. `output.presentationMeta(args, value)` выводит JSON-метаданные для прямых верхнеуровневых вызовов; эти метаданные переживают `tool/result` и возвращаются в `presentResult`, тогда как само каноническое значение остаётся локальным для исполнения и никогда не воспроизводится. Вложенные диспетчеризации Code Mode метаданные не вычисляют. `defineTool` мягко валидирует старые записанные аргументы и откатывается к запасному представлению, а не валит воспроизведение. Эталонные реализации — `dsh-tool-bash` и `dsh-tool-fs`; разделение значения и презентации описывает [Agent Note о каноническом выводе](../../../.agents/notes/implemented/architecture/2026-07-20-canonical-tool-output-contract.md), а словарь карточек — [Agent Note о намерении рендера](../../../.agents/notes/implemented/architecture/2026-07-02-tool-render-intent-union.md).

### Code Mode

При `code` или `both` реестр выставляет зарезервированный транспорт `run_code` и детерминированный SDK для текущего scope, порождаемый на языке загруженного рантайма, — рендерер выбирается по `ctx.codeRuntime.language` (`typescript` → приведённый ниже TypeScript SDK, `python` → Python SDK). SDK декларирует точные типы аргументов и канонического вывода для каждого видимого инструмента (`ToolArgsMap`/`ToolOutputMap` в TypeScript, именованные `TypedDict` в Python), и каждая привязка резолвится в каноническое JSON-значение инструмента. Каждый беспотерьный JSON-вызов привязки заново входит в полный конвейер инструментов под нативным контрактом планирования (безопасные для конкурентности вызовы могут перекрываться до `maxParallelSubCalls`; эксклюзивные исполняются в одиночку как барьеры порядка) с журнальной корреляцией к внешнему вызову. Отказы и другие неудачные результаты реджектятся с настоящей видимой программе `ToolCallError`, несущей только `toolName` и `message`; нативное содержимое и внутренние коды ошибок остаются за пределами контракта Code. Логи и возвращаемое значение программы заново входят в контекст модели; когда финальный нативный контент успешно завершившегося подвызова содержит изображение, мост также проводит это полное упорядоченное содержимое через родительский результат, чтобы изображение не потерялось за JSON-привязкой. Финальная блокировка post-execute или замена содержимого авторитетны. Обычные побочные эффекты не откатываются, а `additionalContexts` подвызовов проводятся через родительский результат, сохраняя смежность вызова и результата. Завершение прогона отменяет и высушивает незакрытые привязки; сбои рантайма проявляются как `CodeRunFailedError`.

При `code` — в отличие от `both` — транспорт к тому же единственная точка входа, доступная модели: прямой вызов модели, называющий другой видимый инструмент, сворачивается в `UNKNOWN_TOOL` при создании исполнения, раньше `tools/pre-execute`, одобрения `ask` и стражей, так что никто не наблюдает и не одобряет вызов, которому гарантирован провал. Отказ называет маршрут обратно (`only \`run_code\` is callable directly — call \`<name>\` from inside a \`run_code\` program instead`), потому что тот же промпт объявляет этот инструмент, а голое «unknown tool» читается как сломанное развёртывание. Поддиспетчеризации SDK несят `parent`-токен внешнего исполнения и освобождены от этого, поэтому программы сохраняют каждую объявленную SDK привязку. См. [замечание о схлопывании исполнителя](../../../.agents/notes/implemented/bug-fix/2026-08-07-code-mode-executor-collapse.md), [основание Code Mode](../../../.agents/notes/implemented/feature/2026-06-15-code-mode.md), [контракт типизированных возвратов](../../../.agents/notes/implemented/feature/2026-07-20-code-mode-typed-tool-returns.md) и [code-runtime seam](../../code-runtime/README.md). Попробуйте `pnpm run demo:code-mode`.

- **Секция SDK** (`tools:sdk`, порядок 150): ленивая секция промпта, заново генерирующая текст SDK на нужном языке при каждой сборке. Во вкусе TypeScript она выдаёт `JsonValue`, точные `ToolArgsMap` / `ToolOutputMap`, `ToolName`, объявление `ToolCallError` и отображённый namespace `tools` для видимых конечных возможностей scope вызывающего (экзотические имена — через ключи в кавычках), плюс фиксированные инструкции по использованию; вкусу Python (`ctx.codeRuntime.language === 'python'`) соответствуют именованные `TypedDict` и объект `tools` с теми же инструкциями по использованию. Детерминированно: лексикографический порядок инструментов, побайтно идентичный текст для неизменного набора инструментов (дружественно к префиксному кешу). Оба кодогенератора экспортируются и никогда не бросают исключений при сборке промпта: `jsonSchemaToTs` обрабатывает любую конструкцию единой схемы и деградирует неподдерживаемые сырые конструкции до `unknown`; `jsonSchemaToPy` делает то же, деградируя до `Any` (и целого объекта до `dict[str, Any]`, когда имя поля — нелегальный атрибут `TypedDict`, либо всякий раз вне рендера SDK, который поставляет контекст именования, необходимый объявлению `TypedDict`).
- **Мост диспетчеризации** (execute у `run_code`): каждый вызов привязки снимается в беспотерьный JSON до диспетчеризации (`undefined`, `BigInt`, циклы, разреженные массивы, `-0` и экзотические объекты отвергают именно этот вызов), планируется через пул на прогон, переиспользующий нативный контракт конкурентности, — вызовы стартуют строго в порядке подачи, подряд идущие вызовы с `isConcurrencySafe` перекрываются до валидированного конфигурацией `maxParallelSubCalls` (по умолчанию 10; `1` возвращает серийную диспетчеризацию), а вызов с эксклюзивной классификацией высушивает пул, исполняется один и закрывает дорогу последующим, — получает непрозрачный токен внешнего исполнения как `parent` и проходит полный конвейер pre-execute → стражи → execute → post-execute → result. Успех возвращает финальное каноническое значение после политики; неудача доходит до воркера одним сообщением и становится `ToolCallError(toolName, message)`. Каждый стартовавший подвызов журналирует на входе конвейера событие `tool/code-dispatch-start` (детерминированный id `<parent>:code:<n>`, нумерация по порядку подачи) и завершается одним событием `tool/code-dispatch`, несущим полный видимый модели исход `content`/`isError` (словарь `tool/result`, поэтому UI рисует подвызовы через нативный путь — поля `time` пары несут тайминги отдельных подвызовов); вызов, брошенный в очереди завершением прогона, не журналирует ни того ни другого. `deriveMessages()` не выставляет ни одного из этих событий и не персистит каноническое значение. Корреляция токенов позволяет наблюдателям в стиле фиксации отложить внутренний успех до финального результата `run_code`, не раскрывая живое внешнее исполнение; обычные побочные эффекты инструментов не откатываются. Каждая запись `additionalContexts` подвызова и каждая успешная финальная последовательность содержимого с изображением проводятся через внешний `ToolRunContext` в порядке диспетчеризации; цикл дописывает эти контексты только после родительского результата `run_code`, сохраняя смежность и атрибуцию источника, даже если программа затем падает.
- **Дисциплина завершения**: мост владеет ограниченной прогоном отменой, следующей за внешним сигналом внутрь и срабатывающей, когда прогон завершается по любой причине, поэтому исчерпание бюджета отменяет летящий под-инструмент, вместо того чтобы бросить его осиротевшим; затем мост ДО возврата высушивает свою очередь, так что каждое `tool/code-dispatch` попадает в открытый ход. Неудачный прогон бросает `CodeRunFailedError` (`code: 'CODE_RUN_FAILED'`, message = род сбоя + захваченные логи), который конвейер превращает в структурированный `isError`, по которому модель сама себя исправляет.
- **Размер результата**: промежуточные значения привязок пересекают процесс воркера целиком и не имеют побайтового потолка на привязку. `run_code` возвращает каноническое `{ logs: string[], result?: JsonValue }`; строки рисуются как есть, любой другой присутствующий JSON-корень рисуется стеково-безопасной красивой JSON-трассировкой с общим отступом не выше десяти символов (глубинные поддеревья остаются компактными), `null` остаётся явным, а отсутствие `result` означает, что программа вернула `undefined`. Настраиваемый `maxOutputBytes` воркера (по умолчанию 64 МиБ) применяется только к суммарно сериализуемым полезным нагрузкам внешнего массива логов, значения завершения или сообщения о сбое; фиксированный синтаксис конверта результата и презентационные пробелы вне этого лимита. Невалидные и сверхлимитные завершения валятся явно, и лишь этот внешний результат допускается к обычному spill.

### Параллельное исполнение

Цикл агента собирает подряд идущие `parallel`-вызовы в ограниченный скользящий пул и трактует каждый `exclusive`-вызов как барьер порядка. Перекрываются только диспетчеризация и тело; политика, долговечные результаты и контекст сохраняют порядок модели. Привязки Code Mode переиспользуют ту же классификацию через собственный пул моста. Штатные объявления и обоснования описывает [Agent Note о параллельных вызовах инструментов](../../../.agents/notes/implemented/feature/2026-07-10-parallel-tool-call-execution.md).

## Model Experience

### Обычные схемы инструментов

#### What the model sees

В обычном режиме модель видит точное имя, описание и JSON-схему каждого видимого определения; штатные определения записаны в генерируемой [карте пакетов инструментов и секциях схем](../../../docs/tool-catalog.md#tool-package-map). Ограничения, перекрытия и расширения в объёме агента меняют набор конечных инструментов именно этого агента.

#### Token effect

Фиксированная стоимость на запрос, пропорциональная видимым определениям. Ограничения, скрывающие инструменты, убирают всю стоимость их схем для этого агента.

#### KV Cache effect

Префикс стабилен, пока видимые определения и их порядок неизменны. Регистрация, освобождение ресурсов или ограничение scope способны инвалидировать переиспользование от первого изменившегося токена схемы.

### Схема и системный промпт Code Mode

#### What the model sees

Code Mode выставляет генерируемую [схему `run_code`](../../../docs/tool-catalog.md#deepseek-aidsh-tools), приведённые ниже инструкции SDK и сгенерированный точный SDK-блок на языке загруженного рантайма (TypeScript-блок `declare const tools` либо Python-объявление `tools`). `both` выставляет обычные схемы и этот API Code Mode. При `code` промпт вдобавок несёт правило `tools:code-only`, упорядоченное впереди блока пер-инструментальных наставлений, чтобы модель прочитала, какие инструменты ей доступны, прежде чем узнает, зачем каждый нужен; `both` рисует его пустым. Инструкции и SDK-блок соответствуют языку загруженного рантайма; ниже показана версия TypeScript (через [`dsh-code-runtime-worker-thread`](../../code-runtime/code-runtime-worker-thread/README.md)), а версия Python (для любого рантайма, сообщающего `language: 'python'`) предлагает те же операции и типы в питоновском синтаксисе (`await tools.name(args)`, доступ по индексу для экзотических имён, `print(...)` и верхнеуровневый `return`).

##### Инструкции SDK Code Mode

```markdown
## Writing code for run_code

`run_code` takes two required arguments: `code` — the body of an async TypeScript function (erasable syntax only — no `enum` or namespaces; type annotations are advisory, the code runs type-stripped) — and `description`, a short summary of what the program does. Inside the program:

- Call tools as `await tools.name(args)` — quoted access for exotic names: `tools["my-tool"](args)`. Every call resolves to the tool's typed canonical JSON value. Tool arguments must be lossless JSON.
- A FAILED tool call rejects with `ToolCallError`, whose `toolName` identifies the failed tool and whose `message` is human-readable — `try/catch` it to handle and continue.
- Independent read-only calls MAY overlap under `Promise.all` (safe calls run concurrently; mutating calls run alone, in submission order). Sequence dependent work with `await`.
- Emit results with `return` and/or `console.log(...)`. ONLY what you print or return comes back to you — intermediate tool results never enter the conversation, so extract just what you need.

The available tools:
```

#### Token effect

Фиксированная стоимость на запрос, пропорциональная видимым определениям. Code Mode разменивает схемы конечных инструментов на сгенерированный текст SDK плюс одну схему транспорта, не обещая универсального сокращения.

#### KV Cache effect

Префикс стабилен, пока неизменны выбор Code Mode, сгенерированный SDK, схема транспорта и набор видимых инструментов. Смена режима или фильтра способна инвалидировать переиспользование от первого изменившегося токена промпта или схемы.

### История вызовов инструментов и результаты

#### What the model sees

Цикл удерживает выпущенные моделью аргументы и финальный контент реестра. Любой брошенный или отклонённый вызов становится ровно `Error: <message>`. Code Mode рисует напечатанные строки внешней программы и её возвращаемое значение, `(run_code completed with no output)`, когда пусты оба, или `Error: code run failed (<kind>): <message>` с условным продолжением `Captured output:` и захваченными строками. Внутренние события диспетчеризации остаются только в журнале, тогда как успешный под-результат с изображением дописывается после внешнего результата как контекст с атрибуцией источника; слушатели post-execute могут дописать иной контекст с атрибуцией на той же границе.

#### Token effect

Аргументы, результаты и дополнительный контекст зависят от данных и пересылаются до компакции. Ограничения, скрывающие инструменты, убирают и их схемы прежде, чем модель успеет их вызвать.

#### KV Cache effect

Append-only; новое видимое содержимое следует за переиспользуемым префиксом запроса и не инвалидирует существующие записи KV-кеша.

## Known Limitations and Deferred Work

- **Политика конкурентности — не событийная решающая точка** — `executionMode()` читает разрешённое определение инструмента напрямую; плагины способны объявить классификатор только на определениях, которыми владеют.
- **`tools/pre-execute` намеренно не может переписать `exec.arguments`** — записанные и отрисованные аргументы разошлись бы с тем, что исполнилось; дизайн переписывания — [предлагаемый Agent Note](../../../.agents/notes/proposed/feature/2026-06-30-pre-tool-input-rewrite.md).
- **Структурированные выводы субагентов и воркфлоу, задаваемые вызывающим, остаются объектно-корневыми** — это защита на уровне потребителей; общий словарь схем и выводы инструментов поддерживают любой JSON-корень.
- **`timeoutMs` на определении — только декларация** — реестр никогда не следит за сроками; принуждение требует обёртки `@deepseek-ai/dsh-tool-call-timeout-policy`.
- **Язык SDK Code Mode следует за единственным загруженным рантаймом, а предъявление действует на агента, а не на инструмент** — `mode: code`/`both` отвергает сборку промпта, пока у `ctx.codeRuntime.language` нет зарегистрированного рендерера SDK (TypeScript или Python); ограничения/перекрытия в объёме scope и `presentAs` избирают каждому агенту его видимые привязки и их форму, но внутри одного агента ни один инструмент не может быть только нативным, пока другой только кодовым.
- **Промежуточные значения Code Mode локальны для исполнения и не ограничены байтами** — канонические типизированные значения нельзя восстановить из воспроизведения сессии, и они способны исчерпать память процесса или воркера; настраиваемый жёсткий потолок воркера имеет только внешний вывод `run_code`. Долговечная журнальная копия каждого подвызова ОГРАНИЧЕНА: каскад `tools/code-dispatch-log` позволяет политике spill заменить переразмерное содержимое `tool/code-dispatch` превью + указателем ([rationale](../../../.agents/notes/implemented/feature/2026-07-26-code-dispatch-log-spill.md)).
- **Состояние `run_code` свежо на каждый прогон** — постоянное REPL-ядро отвергнуто для MVP (межвызовное состояние было бы невидимо журналу); см. [Agent Note о Code Mode](../../../.agents/notes/implemented/feature/2026-06-15-code-mode.md).
