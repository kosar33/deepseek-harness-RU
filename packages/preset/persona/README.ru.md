# dsh-persona

[English](README.md) | [中文](README.zh.md) | Русский

Персона агента как компонуемая строка. Она может либо затенять персону развёртывания, либо владеть полным системным промптом.

[`dsh-system-prompt`](../../core/system-prompt/README.ru.md) владеет персоной развёртывания как собственной конфигурацией и регистрирует эту секцию безусловно, так что у процесса ровно одна персона. [Пресет агента](../agent-presets/README.ru.md) не может монтировать сам реестр промптов — без собственной строки пресет мог бы менять инструменты агента, но никогда его идентичность. Этот пакет и есть такая строка.

## Только внутри scope

Монтирование этой строки вне scope агента сталкивается с собственной регистрацией `deployment:persona` реестра и завершается громким сбоем. Это не ограничение, которое стоит обходить: у персоны развёртывания уже есть владелец, и весь смысл этой строки — затенить её для одного агента. Монтируйте её внутри композиции пресета, где монтирование пресета снабжает строку scope'ом агента.

## Конфигурация

| Поле | Умолчание | Смысл |
|---|---|---|
| `text` | обязательно | Проза персоны, рендеримая как секция `deployment:persona` |
| `complete` | `false` | Восстановить после сборки эту персону как единственную секцию системного промпта |
| `includeRuntimeContext` | `true` | Включить динамические снимки runtime-контекста для этого scope агента; false подавляет каждый вклад контекста, не отключая владеющие им сервисы |

`text` — шаблон, как всякая секция промпта: полные группы `{{…}}` разрешаются строго против зарегистрированных переменных промпта в момент рендера, а не сборки. Пустой текст всё равно занимает slot, так что он полностью затеняет персону развёртывания, а затем исчезает при рендере. С `complete: true` сборка по-прежнему разрешает контексты, инструменты, переменные и кооперативных слушателей, а затем реестр промптов восстанавливает эту точную персону единственной секцией; ни идентичность, ни руководство по инструментам, ни слушатель не могут добавить текст в промпт. С `includeRuntimeContext: false` провайдеры контекста не вычисляются для этого scope, а контексты, добавленные слушателями сборки, отбрасываются.

## Влияние на модель

### Секция персоны

#### Что видит модель

Секцию `deployment:persona` с порядком 0, сразу после открывателя идентичности harness, несущую ровно сконфигурированный `text` этой строки с разрешёнными переменными промпта. Для агента, чей пресет монтирует эту строку, она заменяет любую персону, сконфигурированную развёртыванием. В режиме complete модель видит только эту отрендеренную секцию как свой системный промпт. Runtime-контекст включён по умолчанию. При отключении свежий агент не получает никакого снимка runtime-контекста — ни от политики песочницы, ни от политики разрешений, ни от делегирования, ни от другого провайдера контекста системного промпта.

#### Влияние на токены

Фиксировано для данного пресета: собственные токены персоны в каждом запросе этого агента и ни одного — для любого другого агента. Пустой текст не вносит ничего. Режим complete убирает все прочие токены системного промпта у этого агента.

#### Влияние на KV-кэш

Стабильность префикса на всю жизнь агента — строка монтируется однажды, до публикации агента, а значит до его первого запроса, и её текст никогда не меняется, пока агент работает. Два агента на разных пресетах устанавливают разные префиксы начиная с этой секции; ни один не может инвалидировать переиспользование другого.

## Известные ограничения и отложенная работа

- **Нет глобального монтирования** — слот персоны вне scope принадлежит реестру промптов, поэтому эта строка применима только из scoped-композиции. Персональное изменение уровня всего развёртывания принадлежит собственной конфигурации строки `system-prompt`.
