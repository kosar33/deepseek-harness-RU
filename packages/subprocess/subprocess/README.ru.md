# @deepseek-ai/dsh-subprocess

[English](README.md) | [中文](README.zh.md) | Русский

Seam дочерних процессов (`ctx.subprocess`) — процессная половина одного мира исполнения. Абстрактный `SubprocessRuntime` раскрывает поиск исполняемых файлов, обычный управляемый `spawn` и один примитив терминального процесса; его словарь покрывает сырой/собираемый stdio, дескрипторы процессов и терминалов, факты выхода, зачистку дерева/сессии и управляемое пространство имён среды `DSH_*`. Локальная реализация живёт в [`dsh-subprocess-local`](../subprocess-local/README.ru.md).

## Контракт

- `spawn(spec)` немедленно возвращает живой дескриптор; `done` разрешается при закрытии процесса с фактами выхода (`SubprocessOutcome` не несёт ни вывода, ни классификации причин) и отклоняется только из-за отказов уровня spawn.
- Рабочие каталоги запуска и пути исполняемых файлов принадлежат миру исполнения провайдера. `resolveExecutable(command, env?, signal?)` проверяет абсолютные команды или разрешает имена без пути по очищенному PATH этого мира плюс явным переопределениям.
- Спецификация полностью явна — argv, cwd, диспозиции stdio для каждого потока, льготный период, — потому что зависящие от развёртывания умолчания принадлежат конфигурации вызывающего, а не скрытому умолчанию сервиса subprocess (владельческий шаблон — разделение запроса и spec в `dsh-shell`). `argv` никогда не интерпретируется оболочкой; потребитель, которому нужна оболочка, сам передаёт `['bash', '-c', command]`.
- Stdio имеет Node-форму для каждого потока: `'pipe'` отдаёт вызывающему сырой поток для собственной протокольной обрамки (LSP JSON-RPC, ACP ndjson), `'inherit'` пропускает сквозь себя родительский дескриптор для диагностики, а режим collect (`{ maxBytes, spill? }`) буферизует ограниченный хвост с опциональным spill-файлом всего потока. Читатели collect работают с байтовыми смещениями всего потока и ничего не потребляют, поэтому независимые читатели не могут украсть дельты друг друга; чтение, чьё смещение выскользнуло из находящегося в памяти хвоста, помечается `lossy` и указывает на spill-файл, когда тот существует. Собранный вывод остаётся читаемым после settlement.
- Завершение на каждой платформе действует на всё дерево (POSIX отсоединённые группы с фолбэком на прямых потомков; Windows `taskkill /T`): `terminate()` — единственный глагол завершения — эскалирует SIGTERM→grace→SIGKILL (идемпотентен, ведом также сигналом отмены спецификации, no-op, когда дерева уже нет), а `waitForExit(signal?)` наблюдает живость всего дерева, поэтому принадлежащая потребителю лестница демонтажа удерживает каждый ярус до настоящего quiescence — управляющий реагирует, но никогда не классифицирует почему (сроки, лестницы демонтажа и классификация причин принадлежат вызывающим).
- `spawnTerminal(spec)` — единственный непайповый примитив. Его дескриптор владеет настоящим PTY, текстовым вводом-выводом UTF-8, инспекцией и сигнализацией групп процессов переднего плана и одной ожидаемой операцией `terminate()`, доводящей до quiescence каждого члена сессии, которого провайдер ещё способен наблюдать, и завершающей выполняющиеся вызовы дескриптора; провайдеры документируют специфичные для подложки пределы наблюдаемости. Сигнал спецификации отменяет только выделение; опубликованный дескриптор владеет своим временем жизни. Поток вывода заканчивается после стоящего в очереди вывода, когда процесс верхнего уровня завершается, а отказ живого транспорта отклоняет `done`. Эти операции остаются одним подложечным примитивом, потому что обычные пайпы не могут выделить управляющий терминал или зачистить членов терминальной сессии; готовность, scrollback и политика владельца остаются в PTY-потребителе.
- `scrubbedParentEnv()` / `SENSITIVE_ENV_PATTERN` — единое общее определение очистки: похожие на учётные данные имена из окружающей среды и имена `DSH_*` отбрасываются, а явный `env` сливается уже после очистки. Её применяют как локальный обычный, так и терминальный запуск; транспорты, управляемые SDK и владеющие собственным запуском, могут импортировать её напрямую.
- Освобождение сервиса завершает все всё ещё исполняющиеся управляемые процессы и ожидает их выхода.

См. [страницу подсистемы subprocess](../../../docs/subsystems/subprocess.ru.md) и [Agent Note о seam](../../../.agents/notes/implemented/architecture/2026-07-26-subprocess-seam.md).

## Model Experience

Косвенно — через Consumers (сегодня это семейство исполнителей bash за `dsh-tool-bash`), которым принадлежит вся обращённая к модели отрисовка вывода процессов и их жизненного цикла.

#### KV Cache effect

Прямой инвалидации нет; названные потребители владеют любыми изменениями префикса запроса.

## Известные ограничения и отложенная работа

- **Управляемые SDK запуски остаются вовне** — транспорт, управляемый SDK и владеющий собственным внутренним запуском, не может провести этот вызов через данный сервис; он тем не менее может импортировать `scrubbedParentEnv`, чтобы политика среды оставалась в одном источнике.
- **Лестницы демонтажа принадлежат потребителям** — seam поставляет глаголы сигнализации и ожидание живости дерева, а не заготовленную последовательность полного успокоения; каждый внепроцессный потребитель сам кодирует форму сотрудничества своего потомка (лестница бэкенда ACP «сначала stdin-EOF» — шаблон внутри репозитория).
